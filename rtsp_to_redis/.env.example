# Copy this file to `.env` in the same directory for the rtsp_to_redis
# service.  The variables are a subset of those required by the main stack
# in docker/.env.example, so you may simply reuse the same values.

PROMETHEUS_PORT=9101
RTSP_URL=rtsp://<tailscale-ip>/stream
REDIS_URL=redis://redis:6379/0
RAW_FRAME_KEY=raw_frames
FRAME_BUFFER_SIZE=500

# Optional tuning
#FPS=15